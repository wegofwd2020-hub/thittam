version: v1
# Separate from buf.gen.yaml — used only by the nightly API-docs workflow.
# Writes a single markdown file with every service, RPC, and message type.
plugins:
  - plugin: buf.build/community/pseudomuto-doc:v1.5.1
    out: generated-docs
    opt:
      - markdown,PROTO_REFERENCE.md
